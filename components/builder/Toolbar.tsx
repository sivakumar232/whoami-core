'use client';

interface ToolbarProps {
    userId: string;
}

/**
 * Toolbar - Top toolbar (currently empty, undo/redo removed)
 */
export function Toolbar({ userId }: ToolbarProps) {
    return null; // Toolbar removed
}
